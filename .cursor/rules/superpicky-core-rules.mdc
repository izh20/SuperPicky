---
description: SuperPicky 项目 guardrails（编码、跨平台、打包、ExifTool）
alwaysApply: true
---

# SuperPicky Cursor 规则

遵循 `scripts_dev/AI_CODING_RULES.md`。

## 不可协商的原则

- 使用 UTF-8，避免中文乱码。
- 对于 ExifTool 非 ASCII 元数据写入，使用 UTF-8 临时文件重定向（`-Tag<=file`），而不是内联命令值。
- 保持 Windows + macOS 兼容性（文件路径和子进程使用）。
- 在多线程 SQLite 代码中，不要仅依赖 `check_same_thread=False`；应对共享连接进行序列化访问或使用每线程连接。
- 严禁在业务逻辑中绕过数据库封装器直接调用私有连接（例如 `report_db._conn.*`）。
- 保持事务处理同步和防御性（避免混合不同步的事务风格；仅在有效时提交）。
- 确保持久化进程能被干净且幂等地关闭。
- 对于打包专用的 CUDA 错误，首先诊断打包/运行时问题。
- Torch/CUDA Windows 打包保持 UPX 禁用，除非经过明确验证。

## 验证

- 对修改的 Python 文件运行 `py -3 -m py_compile`。
- 触碰 EXIF 相关逻辑时，验证元数据写入/读取回显。
- 修改 `.spec` 或打包逻辑时，运行打包应用冒烟测试。
- 修改数据库/线程逻辑时，运行多线程数据库压力测试以捕获事务状态回归。
